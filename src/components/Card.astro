---
export interface Props {
	title: string
}

const { title } = Astro.props
---

<article class="w-80 rounded border border-black bg-white p-4">
	<div class="flex h-40 justify-center">
		<slot name="thumbnail" />
	</div>
	<header class="text-center font-semibold">{title}</header>
</article>
